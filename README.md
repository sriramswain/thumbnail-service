# thumbnail-service
Production-style REST API that accepts image uploads and generates resized thumbnails. Supports preset sizes and custom dimensions while preserving aspect ratio. Returns metadata and allows retrieval of generated images. Designed with validation, async processing, tests, and clean modular structure.
